'use client';

import { PlaceholderPage } from '@/components/layout/placeholder-page';

export default function GeneralLedgerPage() {
  return (
    <PlaceholderPage
      title="Buku Besar"
      description="Fitur ini memerlukan implementasi logika akuntansi lengkap untuk menghasilkan buku besar dari jurnal umum. Ini akan dikembangkan pada tahap berikutnya."
    />
  );
}
