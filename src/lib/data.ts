import type { Product, Transaction, SalesData, TopProductData, MonthlyRevenue, Customer, Supplier } from './types';

// Mock data is no longer used and has been removed.
// The application now fetches data directly from Firebase Firestore.

export const mockProducts: Product[] = [];
export const mockTransactions: Transaction[] = [];
export const mockWeeklySales: SalesData[] = [];
export const mockTopProducts: TopProductData[] = [];
export const mockMonthlyRevenue: MonthlyRevenue[] = [];
export const mockCustomers: Customer[] = [];
export const mockSuppliers: Supplier[] = [];
